export default function Footer() {
  return (
    <footer>
      <p>wip quote generator ✨</p>
    </footer>
  );
}

// change footer message to a motivational quote per click
// require API
